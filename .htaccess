# Disable directory listing
Options -Indexes

# Deny access to sensitive files
<FilesMatch "\.(env|sql|md|json|lock)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Allow composer.json (Railway needs it for detection)
<Files "composer.json">
    Order allow,deny
    Allow from all
</Files>
